export default function htmlToElement (htmlElement) {
  const template = document.createElement('template');
  template.innerHTML = htmlElement;
  return template.content.firstChild
}

// export default htmltoElement;